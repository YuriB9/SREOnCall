# Задачи: Устранение замечаний оболочки аутентификации

## 1. Согласование логики членства (Замечание 2)

- [ ] 1.1 Переписать `parseGroups` в `frontend/src/auth/usePermissions.ts`: трактовать `/{tenant}` и `/{tenant}/<любой-сегмент>` как `member`, `/{tenant}/admins` — как `admin` (зеркало серверного `IsMember`/`IsAdmin` в `pkg/auth/auth.go`)
- [ ] 1.2 Проверить, что `TenantGuard`, `AdminGuard` и `SelectTeamPage` корректно работают с обновлённой картой (admin подразумевает member-доступ)
- [ ] 1.3 Добавить юнит-тесты `parseGroups` (несколько тенантов, префиксная подгруппа без отдельной записи тенанта, admin, пустой/отсутствующий `groups`) — когда появится тест-инфраструктура (backlog бывшей секции 10)

## 2. Диагностика permissions (Замечание 1)

- [ ] 2.1 В потребителе `usePermissions()` (страница «Нет доступа к командам»/403) различать состояния: пустой `groups` → обычное «нет членства»; непустой `groups` при пустой карте → диагностическое сообщение о вероятной неверной настройке Keycloak-маппера
- [ ] 2.2 Логировать в консоль исходный `groups` и причину при срабатывании диагностики (без вывода токена целиком)

## 3. Graceful-обработка недоступности Keycloak (Замечание 3)

- [ ] 3.1 Создать переиспользуемый компонент экрана ошибки аутентификации с заголовком, текстом и кнопкой действия (повтор входа)
- [ ] 3.2 `CallbackPage.tsx`: на ошибке `signinRedirectCallback()` рендерить экран «Не удалось завершить вход» с кнопкой «Повторить вход» вместо `navigate('/')`; логировать оригинальную ошибку
- [ ] 3.3 Обернуть запуск входа в `RequireAuth` обработкой ошибки `signinRedirect`: при сбое показывать экран «Сервис авторизации недоступен» с кнопкой повтора (а не пустой экран)
- [ ] 3.4 (Опц.) Добавить React error-boundary вокруг точки входа для непредвиденных исключений рендера

## 4. Устойчивость баннера сессии (Замечание 4)

- [ ] 4.1 `SessionBanner.tsx`: не запускать 30-секундный отсчёт по первому `silentRenewError`; триггерить баннер только при приближении `exp` (через `user.expires_at`, порог 120 сек) или после N подряд неудачных попыток обновления
- [ ] 4.2 Вынести порог N в именованную константу; сбрасывать счётчик неудач на событие `userLoaded`
- [ ] 4.3 Убедиться, что спек-гарантия сохранена: баннер появляется до фактического истечения токена, успешное обновление скрывает баннер

## 5. Документация требований Keycloak (Замечание 1)

- [ ] 5.1 Зафиксировать в README фронтенда (и/или деплой-доке) требование наличия groups-маппера в `id_token` И `access_token`, с объяснением причины (фронт читает из id_token, бэкенд — из access_token)
- [ ] 5.2 Указать связь со scope/client scope Keycloak (`groups` не покрывается `openid profile email` по умолчанию)

## 6. Проверка

- [ ] 6.1 Прогнать `tsc -b` (сборка без ошибок типов) после изменений
- [ ] 6.2 Ручная проверка сценариев: вход при недоступном Keycloak, ошибка на `/callback`, транзиентный сбой renew (баннер не появляется), членство по префиксной подгруппе
