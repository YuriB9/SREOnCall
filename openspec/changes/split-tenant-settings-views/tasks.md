## 1. Разбиение страниц настроек (frontend)

- [ ] 1.1 Выделить секции `WebhookTokensSection`, `NotificationConfigSection`, `MembersSection` в переиспользуемые компоненты (вынести в `frontend/src/pages/settings/` или оставить в модуле, экспортировав)
- [ ] 1.2 Создать тонкие страницы-обёртки `WebhookTokensPage`, `NotificationConfigPage`, `MembersPage`, каждая читает `tenant` из `useParams` и рендерит свою секцию
- [ ] 1.3 Удалить старый `TenantSettingsPage`, рендеривший все три секции стопкой, и убрать его импорт

## 2. Маршрутизация (frontend)

- [ ] 2.1 В `frontend/src/routes.tsx` заменить одиночный маршрут `settings` на родительский маршрут с `AdminGuard` + `Outlet` и дочерними `webhook-tokens`, `notifications`, `members`
- [ ] 2.2 Добавить `index`-маршрут под `settings` с редиректом `<Navigate to="webhook-tokens" replace />`
- [ ] 2.3 Убедиться, что `AdminGuard` покрывает все подмаршруты через родителя (без дублирования на каждом)

## 3. Навигация (frontend)

- [ ] 3.1 В `frontend/src/layouts/GlobalLayout.tsx` заменить `NavLink` «Настройки» на разворачиваемую группу: кнопка-тогглер с локальным состоянием раскрытия
- [ ] 3.2 Добавить подменю с тремя `NavLink`: «Webhook-токены» → `settings/webhook-tokens`, «Конфигурация уведомлений» → `settings/notifications`, «Участники команды» → `settings/members`, со стилем вложенности и подсветкой активного подпункта
- [ ] 3.3 Инициализировать состояние раскрытия открытым, если текущий путь начинается с `/[tenant]/settings`; сохранить скрытие всей группы для не-администраторов

## 4. Верификация

- [ ] 4.1 Прогнать typecheck/линт фронтенда (`npm run build` / `tsc`) — без ошибок
- [ ] 4.2 Проверить вручную: клик по «Настройки» раскрывает подменю; каждый подпункт открывает свой экран; `/[tenant]/settings` редиректит на `webhook-tokens`; не-админ получает 403 на всех подмаршрутах и не видит пункт в навигации
- [ ] 4.3 Проверить и при необходимости обновить E2E-тесты навигации/настроек в `tests/e2e`
