## MODIFIED Requirements

### Requirement: Срабатывание эскалации по таймауту

Сервис escalation ДОЛЖЕН (SHALL) отслеживать активные состояния эскалации и переходить на следующий уровень, если текущий таймаут истёк без подтверждения.

Запросы к сервису scheduling за текущим дежурным ДОЛЖНЫ выполняться с сервисной аутентификацией (заголовок `X-Admin-Key` с ключом из конфигурации сервиса). При сбое запроса дежурного (сетевая ошибка, не-2xx ответ) сервис ДОЛЖЕН залогировать структурированную ошибку с уровнем `error` и причиной; событие `escalation.triggered` с пустыми `oncall_user_id`/`oncall_username` НЕ ДОЛЖНО публиковаться молча — пустые on-call-поля допустимы только с одновременной error-записью в лог, чтобы сбой авторизации был наблюдаемым.

#### Scenario: Инцидент не подтверждён в течение таймаута

- **WHEN** инцидент с активной политикой эскалации не подтверждён в течение `timeout_minutes` текущего уровня
- **THEN** сервис escalation запрашивает scheduling (`GET /schedules/{notify_schedule_id}/oncall`) за текущим дежурным, переходит на следующий уровень и публикует событие `escalation.triggered` с полями `incident_id`, `tenant_id`, `tier`, `oncall_user_id`, `oncall_username`

#### Scenario: Запрос дежурного выполняется с сервисной аутентификацией

- **WHEN** сервис escalation выполняет запрос к scheduling за текущим дежурным
- **THEN** запрос содержит заголовок `X-Admin-Key`, и scheduling принимает его без пользовательского JWT, возвращая HTTP 200 с данными дежурного

#### Scenario: Сбой резолва дежурного наблюдаем

- **WHEN** запрос к scheduling за дежурным завершился ошибкой (401, сетевой сбой, 5xx)
- **THEN** сервис логирует ошибку с уровнем `error`, указанием `schedule_id` и причины; если событие `escalation.triggered` всё же публикуется с пустым `oncall_user_id`, это сопровождается той же error-записью

#### Scenario: Инцидент подтверждён до истечения таймаута

- **WHEN** инцидент переходит в статус `acknowledged` до истечения таймаута уровня
- **THEN** переход эскалации для данного инцидента останавливается

#### Scenario: Все уровни исчерпаны

- **WHEN** все уровни были уведомлены, а инцидент так и не подтверждён
- **THEN** состояние эскалации устанавливается в `exhausted`, публикуется событие `escalation.exhausted`; дальнейшая автоматическая эскалация не происходит
