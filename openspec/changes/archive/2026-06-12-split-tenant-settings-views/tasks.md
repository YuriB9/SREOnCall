## 1. Разбиение страниц настроек (frontend)

- [x] 1.1 Выделить секции `WebhookTokensSection`, `NotificationConfigSection`, `MembersSection` в переиспользуемые компоненты (вынести в `frontend/src/pages/settings/` или оставить в модуле, экспортировав)
- [x] 1.2 Создать тонкие страницы-обёртки `WebhookTokensPage`, `NotificationConfigPage`, `MembersPage`, каждая читает `tenant` из `useParams` и рендерит свою секцию
- [x] 1.3 Удалить старый `TenantSettingsPage`, рендеривший все три секции стопкой, и убрать его импорт

## 2. Маршрутизация (frontend)

- [x] 2.1 В `frontend/src/routes.tsx` заменить одиночный маршрут `settings` на родительский маршрут с `AdminGuard` + `Outlet` и дочерними `webhook-tokens`, `notifications`, `members`
- [x] 2.2 Добавить `index`-маршрут под `settings` с редиректом `<Navigate to="webhook-tokens" replace />`
- [x] 2.3 Убедиться, что `AdminGuard` покрывает все подмаршруты через родителя (без дублирования на каждом)

## 3. Навигация (frontend)

- [x] 3.1 В `frontend/src/layouts/GlobalLayout.tsx` заменить `NavLink` «Настройки» на разворачиваемую группу: кнопка-тогглер с локальным состоянием раскрытия
- [x] 3.2 Добавить подменю с тремя `NavLink`: «Webhook-токены» → `settings/webhook-tokens`, «Конфигурация уведомлений» → `settings/notifications`, «Участники команды» → `settings/members`, со стилем вложенности и подсветкой активного подпункта
- [x] 3.3 Инициализировать состояние раскрытия открытым, если текущий путь начинается с `/[tenant]/settings`; сохранить скрытие всей группы для не-администраторов

## 4. Профиль в подменю «Настройки» (frontend)

- [x] 4.1 Перенести маршрут профиля под `settings` как `/[tenant]/settings/profile`, вне `AdminGuard` (профиль доступен всем участникам); убрать глобальный маршрут `/profile`
- [x] 4.2 В `GlobalLayout` сделать группу «Настройки» видимой всем пользователям; админские подпункты (`webhook-tokens`, `notifications`, `members`) показывать только админам, подпункт «Мой профиль» — всем
- [x] 4.3 Обновить ссылку с именем пользователя в шапке на `/[tenant]/settings/profile`

## 5. Верификация

- [x] 5.1 Прогнать typecheck/линт фронтенда (`npm run build` / `tsc`) — без ошибок
- [x] 5.2 Проверить вручную: клик по «Настройки» раскрывает подменю; каждый подпункт открывает свой экран; `/[tenant]/settings` редиректит на `webhook-tokens`; «Мой профиль» открывается у админа и у обычного участника; не-админ получает 403 на админских подмаршрутах, но видит «Настройки» → «Мой профиль» в навигации
- [x] 5.3 Проверить и при необходимости обновить E2E-тесты навигации/настроек в `tests/e2e` (E2E — backend API тесты, не зависят от фронтенд-роутинга; обновление не требуется)
