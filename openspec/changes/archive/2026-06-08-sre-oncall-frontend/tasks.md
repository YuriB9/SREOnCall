# Задачи

## 1. Scaffolding проекта

- [x] 1.1 Инициализировать проект Vite + React + TypeScript в директории `frontend/` через `npm create vite@latest`
- [x] 1.2 Установить и настроить Tailwind CSS с `darkMode: 'class'` и Shadcn/ui CLI
- [x] 1.3 Установить `react-router-dom` v6, `@tanstack/react-query` v5, `oidc-client-ts`, `date-fns`, `date-fns-tz`
- [x] 1.4 Настроить path aliases (`@/` → `src/`) в `vite.config.ts` и `tsconfig.json`
- [x] 1.5 Настроить ESLint + Prettier с общей конфигурацией (TypeScript, React, порядок импортов)
- [x] 1.6 Добавить Dockerfile для раздачи статики через nginx и манифест Kubernetes Deployment в `./deploy/k8s/frontend/`

## 2. Оболочка аутентификации

- [x] 2.1 Создать `src/auth/oidcConfig.ts` — настроить `UserManager` с PKCE, `sessionStorage` и параметрами тихого обновления (окно уведомления 120 сек)
- [x] 2.2 Реализовать контекст `<AuthProvider>`, оборачивающий `UserManager`, с экспортом `user`, `signIn`, `signOut` и хука `usePermissions()`
- [x] 2.3 Реализовать хук `usePermissions()`, декодирующий claim `groups` из `id_token` и возвращающий `{ [tenantSlug]: 'member' | 'admin' }`
- [x] 2.4 Создать обработчик маршрута `/callback`, завершающий PKCE-обмен и перенаправляющий на исходный URL назначения
- [x] 2.5 Реализовать обёртку `<RequireAuth>`, перенаправляющую неаутентифицированных пользователей на вход в Keycloak с сохранением `state`
- [x] 2.6 Реализовать баннер истечения сессии, появляющийся за 120 сек до истечения токена и перенаправляющий на вход через 30 сек

<!-- ═══════════════════════════════════════════════════════════════════════════
  БУДУЩАЯ ФИЧА: Устранение замечаний оболочки аутентификации (секция 2)

  КОНТЕКСТ
  ────────
  Аудит секции 2 против бэкенда (pkg/auth/auth.go) показал, что контракт claims
  и формат групп согласованы, существующие сессии переживают падение Keycloak
  (бэкенд кэширует JWKS). Слабые места — связка конфигурации токенов и отсутствие
  graceful-обработки недоступности Keycloak в точке входа/возврата на фронте.

  ЗАМЕЧАНИЕ 1 (ЗНАЧИМОЕ — конфигурационная связка): источник `groups` различается
  ───────────────────────────────────────────────────────────────────────────────
  Фронт читает `groups` из id_token (user.profile в usePermissions.ts:27,
  задача 2.3). Бэкенд читает `groups` из access_token (auth.go:94). Значит маппер
  групп Keycloak ДОЛЖЕН быть добавлен в ОБА токена, иначе где-то ломается
  авторизация (фронт не видит команды ИЛИ бэкенд отдаёт 401/403). Запрошенный scope
  — 'openid profile email' (oidcConfig.ts:7), отдельного groups-scope нет; всё
  держится на client scope в Keycloak.

  Действия:
  — Зафиксировать требование к настройке Keycloak (groups mapper в id_token и
    access_token) в README/деплой-доке.
  — Опционально добавить runtime-проверку: если usePermissions() вернул пустую
    карту при непустом groups в access_token — показать диагностическое сообщение.

  ЗАМЕЧАНИЕ 2 (МИНОРНОЕ): расхождение в трактовке подгрупп
  ──────────────────────────────────────────────────────────
  Бэкенд считает участником любого с префиксом `/tenant/...` (auth.go:39).
  Фронтовый parseGroups регистрирует тенант только по `/tenant` или
  `/tenant/admins`; группу вида `/tenant/other` игнорирует → фронт может скрыть
  тенант, к которому бэкенд доступ даёт. Для схемы `/team` + `/team/admins`
  расхождения нет. Согласовать логику parseGroups с бэкендом (трактовать любой
  префикс `/tenant/` как членство) либо задокументировать ограничение.

  ЗАМЕЧАНИЕ 3 (ЗНАЧИМОЕ — UX при сбое): Keycloak недоступен на входе/возврате
  ──────────────────────────────────────────────────────────────────────────────
  — RequireAuth → signIn() → signinRedirect уводит на authorize-endpoint Keycloak;
    при недоступности пользователь видит ошибку браузера, дружелюбного экрана
    «сервис авторизации недоступен» нет.
  — CallbackPage.tsx:20 на любой ошибке делает navigate('/'), что снова триггерит
    RequireAuth → signIn → потенциальный ЦИКЛ редиректов без объяснения.

  Действия:
  — На ошибке в CallbackPage показывать экран-заглушку с сообщением и кнопкой
    «Повторить вход» вместо немедленного navigate('/') (разорвать цикл).
  — Добавить error-boundary/заставку «Сервис авторизации недоступен» для сбоя
    signinRedirect.

  ЗАМЕЧАНИЕ 4 (МИНОРНОЕ): баннер сессии реагирует на транзиентные ошибки renew
  ─────────────────────────────────────────────────────────────────────────────
  SessionBanner показывается на ЛЮБОЕ событие silentRenewError (SessionBanner.tsx:39),
  включая кратковременный сетевой сбой, и через 30 сек принудительно уводит на
  вход. Добавить троттлинг/повтор: не показывать баннер до N неудачных попыток
  подряд либо запускать обратный отсчёт только при реальном приближении exp токена.

  Задачи:
  ───────
  — [ ] F2.1 Задокументировать требование Keycloak: groups mapper в id_token И
        access_token; зафиксировать в README/деплой-доке (Замечание 1)
  — [ ] F2.2 (опц.) Диагностика пустых permissions при непустом groups в токене
  — [ ] F2.3 Согласовать parseGroups с бэкендом: трактовать любой префикс
        `/tenant/` как членство, либо задокументировать ограничение (Замечание 2)
  — [ ] F2.4 CallbackPage: экран-заглушка с «Повторить вход» вместо navigate('/')
        на ошибке — разорвать цикл редиректов (Замечание 3)
  — [ ] F2.5 Error-boundary/заставка «Сервис авторизации недоступен» при сбое
        signinRedirect (Замечание 3)
  — [ ] F2.6 Троттлинг SessionBanner: не уводить на вход по транзиентному
        silentRenewError; учитывать реальное время до exp (Замечание 4)
════════════════════════════════════════════════════════════════════════════ -->

## 3. Роутинг и макет

- [x] 3.1 Описать полное дерево маршрутов в `src/routes.tsx` (`/`, `/select-team`, `/[tenant]/*`, `/profile`, `/callback`, `/403`)
- [x] 3.2 Реализовать `<TenantGuard>` — читает slug тенанта из URL-параметра, проверяет `usePermissions()`, рендерит 403 если не участник
- [x] 3.3 Реализовать `<AdminGuard>` — оборачивает `/[tenant]/settings`, проверяет роль `admin`, рендерит 403 если не администратор
- [x] 3.4 Собрать компонент `GlobalLayout`: боковая навигация (Инциденты, Расписания, Эскалации, Настройки), шапка с названием тенанта, логином пользователя, переключателем темы, переключателем звука
- [x] 3.5 Собрать страницу `/select-team` со списком доступных тенантов из `usePermissions()` с навигацией по клику; автоматический редирект на `/[tenant]/incidents` при единственном тенанте
- [x] 3.6 Сохранять предпочтение тёмной темы в `localStorage` (`oncall.colorScheme`); применять до первой отрисовки через встроенный скрипт в `index.html`
- [x] 3.7 Реализовать хук `useKeyMap` для глобальной диспетчеризации горячих клавиш (пропускает события, когда input/textarea в фокусе)

## 4. Слой API-клиента

- [x] 4.1 Создать `src/api/client.ts` — обёртка над axios, подставляющая `Authorization: Bearer <token>` из `sessionStorage` в каждый запрос
- [x] 4.2 Написать TypeScript-типы для всех форм ответов бэкенда (Incident, Alert, Comment, HistoryEntry, Schedule, Override, EscalationPolicy, WebhookToken, NotificationConfig, UserContacts)
- [x] 4.3 Создать модуль query-хуков `src/api/incidents.ts`: `useIncidents(tenant, filters)`, `useIncident(tenant, id)`, `useIncidentAlerts`, `useIncidentHistory`, `useIncidentComments`
- [x] 4.4 Создать mutation-хуки: `useAcknowledgeIncident`, `useResolveIncident`, `usePostComment` — с оптимистичными обновлениями и откатом
- [x] 4.5 Создать query-хуки `src/api/schedules.ts`: `useSchedules(tenant)`, `useOnCallNow(tenant)`, `useScheduleWindow(tenant, scheduleId, from, to)`
- [x] 4.6 Создать mutation-хуки: `useCreateOverride`, `useDeleteOverride` с извлечением ошибки 409-конфликта
- [x] 4.7 Создать query/mutation-хуки `src/api/escalations.ts`: `useEscalationPolicies`, `useCreatePolicy`, `useUpdatePolicy`, `useDeletePolicy`, `useSetDefaultPolicy`
- [x] 4.8 Создать query/mutation-хуки `src/api/tenantSettings.ts`: `useWebhookTokens`, `useCreateToken`, `useRevokeToken`, `useNotificationConfig`, `useSaveNotificationConfig`, `useMembers`
- [x] 4.9 Создать query/mutation-хуки `src/api/profile.ts`: `useUserContacts`, `useSaveUserContacts`

## 5. Дашборд инцидентов

- [x] 5.1 Собрать `IncidentListPage` с таблицей Shadcn/ui; колонки: бейдж критичности, Название, чип статуса, Источник, Создан, Подтверждён кем
- [x] 5.2 Добавить панель фильтров с мультиселектом статуса (open/acknowledged/resolved), селектором критичности и текстовым полем источника; синхронизация фильтров с URL search params
- [x] 5.3 Настроить `refetchInterval: 12_000` в `useIncidents` и реализовать аудиотриггер нового инцидента (сравнение наборов ID между запросами)
- [x] 5.4 Реализовать хук `useAudioNotification` — синтезатор сигнала через Web Audio API; читает/записывает `oncall.audioEnabled` в `localStorage`; пропускает при `document.hidden`
- [x] 5.5 Собрать компонент drawer `IncidentDetailPanel` с `Tabs` (Алерты, История, Комментарии)
- [x] 5.6 Собрать вкладку «Алерты»: список алертов с бейджами лейблов «ключ: значение» и индикатором firing/resolved
- [x] 5.7 Собрать вкладку «История»: компонент вертикального таймлайна, рендерящий изменения статуса, события ACK, триггеры эскалации и комментарии в хронологическом порядке
- [x] 5.8 Собрать вкладку «Комментарии»: список сообщений в стиле чата + textarea + кнопка отправки; привязать к mutation `usePostComment`
- [x] 5.9 Добавить кнопки «Подтвердить» и «Закрыть» с состоянием загрузки, логикой блокировки по статусу инцидента и привязкой оптимистичных обновлений
- [x] 5.10 Синхронизировать ID выбранного инцидента с URL-параметром `?incident=<id>`; восстанавливать состояние drawer при загрузке страницы из параметра
- [x] 5.11 Подключить горячие клавиши через `useKeyMap`: `A`=подтвердить, `R`=закрыть, `/`=фокус фильтра, `J`/`K`=навигация по списку, `Esc`=закрыть drawer

<!-- ═══════════════════════════════════════════════════════════════════════════
  БУДУЩАЯ ФИЧА: Устранение замечаний дашборда инцидентов (секция 5)

  КОНТЕКСТ
  ────────
  В ходе аудита API-слоя секции 5 выявлены четыре расхождения между фронтом
  и бэкендом: одно критическое (вкладка «Алерты» мертва), одно значимое
  (мультиселект статуса сломан), два минорных (мёртвый код и UUID вместо логина).

  ЗАМЕЧАНИЕ 1 (КРИТИЧЕСКОЕ): GET /alerts не существует в бэкенде
  ───────────────────────────────────────────────────────────────
  `useIncidentAlerts` делает `GET /api/incidents/v1/{tenant}/incidents/{id}/alerts`,
  но бэкенд регистрирует только `POST /incidents/{id}/alerts` (для ingestion-сервиса).
  Chi возвращает 405. В store нет `ListAlerts`. Вкладка «Алерты» всегда пуста.

  Варианты решения (выбрать один):
  A. Добавить в бэкенд:
       store: ListAlerts(tenantID, incidentID) — SELECT FROM incident_alerts
       handler: GET /incidents/{id}/alerts → h.ListAlerts
  B. Встроить alerts в ответ GetIncident (поле `alerts []IncidentAlert`):
       Исключает отдельный запрос, но утяжеляет GetIncident.
  Рекомендация: вариант A, отдельный endpoint.

  ЗАМЕЧАНИЕ 2 (ЗНАЧИМОЕ): Мультиселект статуса не работает
  ──────────────────────────────────────────────────────────
  Фронт: `statusFilter.join(',')` → `?status=open%2Cacknowledged` (один строковый param).
  Бэкенд: `WHERE status = $1` (точное совпадение). Запрос с двумя статусами даёт 0 строк.

  Нужно согласовать контракт — два варианта:
  A. Фронт: отправлять несколько params `?status=open&status=acknowledged`
     Бэкенд: `r.URL.Query()["status"]` возвращает []string → использовать `ANY($n::text[])`
  B. Ограничить UI одним статусом за раз (упростить фильтрацию, убрать мультиселект)
  Рекомендация: вариант A (сохраняет UX).

  ЗАМЕЧАНИЕ 3 (МИНОРНОЕ): Мёртвый код в IncidentFilters
  ────────────────────────────────────────────────────────
  `source?: string` и `limit?: number` определены в интерфейсе, но:
  — UI не предоставляет ввод source
  — Бэкенд не читает ни source, ни limit из query params
  Удалить поля из интерфейса; PageSize бэкенда = 20 (hardcoded default).

  ЗАМЕЧАНИЕ 4 (КОСМЕТИЧЕСКОЕ): acknowledged_by показывает UUID
  ──────────────────────────────────────────────────────────────
  Бэкенд пишет `acknowledged_by = claims.Sub` (UUID). В таблице инцидентов
  отображается UUID вместо читаемого логина.
  Варианты:
  A. (Бэкенд) Хранить `acknowledged_by` как preferred_username из JWT.
  B. (Фронт) Загружать username по UUID через Keycloak Admin API или через
     эндпоинт members — дорогой fetch на каждый инцидент.
  Рекомендация: вариант A — исправить на бэкенде при записи acknowledged_by.

  Задачи:
  ───────
  — [ ] F3.1 (Backend) Добавить `ListAlerts(tenantID, incidentID)` в store и
        `GET /api/incidents/v1/{tenant}/incidents/{id}/alerts` в handler
  — [ ] F3.2 (Frontend) После появления F3.1: убедиться, что `useIncidentAlerts`
        корректно типизирован под ответ ([] вместо 405)
  — [ ] F3.3 Согласовать контракт мультиселекта статуса: обновить handler
        и store (ANY $n) + изменить сериализацию фильтров на фронте
        (отдельные params вместо join(','))
  — [ ] F3.4 Удалить `source` и `limit` из `IncidentFilters` в `src/api/incidents.ts`
  — [ ] F3.5 (Backend) Записывать `acknowledged_by` как preferred_username (из JWT)
        вместо UUID Sub
════════════════════════════════════════════════════════════════════════════ -->

## 6. UI расписаний дежурств

- [x] 6.1 Собрать скелет `SchedulesPage` с виджетом «Кто дежурит сейчас» вверху (поллинг каждые 60 сек через `useOnCallNow`)
- [x] 6.2 Реализовать компонент Gantt-сетки: рендер шапки месяца с колонками дней, по одной строке на ротацию, полосы смен как позиционированные `div`-ы, вычисленные из данных окна
- [x] 6.3 Реализовать клиентское вычисление окна смены через `date-fns-tz` (конвертация UTC-границ смен в локальный часовой пояс, вычисление процентных значений left/width для полос)
- [x] 6.4 Добавить элементы навигации по месяцам (пред/след); повторно запрашивать данные окна расписания при навигации на новый диапазон месяца
- [x] 6.5 Реализовать мобильный резервный вид: скрыть Gantt на брейкпоинте `<640px`; рендерить список предстоящих смен на 7 дней
- [x] 6.6 Рендерить полосы переопределений на Gantt с визуально отличимым паттерном (штриховой фон или другой оттенок)
- [x] 6.7 Собрать `CreateOverrideModal`: пикеры диапазона дат (начало/конец), селектор участника из `useMembers`, отправка через `useCreateOverride`
- [x] 6.8 Обработать ошибку 409-конфликта в `CreateOverrideModal`: показывать встроенную ошибку с деталями конфликтующего окна, оставлять модальное окно открытым

<!-- ═══════════════════════════════════════════════════════════════════════════
  БУДУЩАЯ ФИЧА: Устранение замечаний UI расписаний (секция 6)

  КОНТЕКСТ
  ────────
  Аудит секции 6 выявил два значимых расхождения (одно вызывает RangeError в
  проде) и два куска мёртвого кода. Основные функции Gantt-сетки и on-call
  виджета работают корректно; проблемы сосредоточены в типах и обработке ошибок.

  ЗАМЕЧАНИЕ 1 (ЗНАЧИМОЕ — runtime error): ConflictDetail не совпадает с 409
  ──────────────────────────────────────────────────────────────────────────────
  Бэкенд при 409 возвращает: { "error": "override window overlaps..." }
  Фронт ожидает: { existing_start, existing_end, existing_user }

  В CreateOverrideModal при 409:
    format(new Date(d.existing_start), ...)
    → new Date(undefined) → Invalid Date → RangeError: Invalid time value

  Ошибка вылетает из onError-коллбека; пользователь не видит описание конфликта.

  Варианты:
  A. (Backend) Вернуть детали конфликта в теле 409:
       { "error": "...", "existing_start": RFC3339, "existing_end": RFC3339,
         "existing_user_id": uuid }
     Потребует доработки store.CreateOverride (вернуть конфликтующую запись).
  B. (Frontend) Показывать только backend message ("override window overlaps..."):
       if (err instanceof OverrideConflictError) {
         setConflictError(err.response.data.error ?? 'Конфликт с существующей заменой')
       }
     Быстрое устранение без изменений бэкенда; нет деталей конфликта.
  Рекомендация: B как быстрое исправление, A — в backlog.

  ЗАМЕЧАНИЕ 2 (ЗНАЧИМОЕ): Override тип содержит неверные поля
  ─────────────────────────────────────────────────────────────
  Фронт: start, end, username, display_name, created_by
  Бэкенд: start_at, end_at (нет username/display_name/created_by)
  Сейчас без runtime-последствий (ответ POST не используется), но тип вводит
  в заблуждение. Исправить тип Override в src/api/types.ts.

  ЗАМЕЧАНИЕ 3 (МИНОРНОЕ): Мёртвый код в src/api/schedules.ts
  ────────────────────────────────────────────────────────────
  — useScheduleWindow: вызывает /oncall вместо /shifts; хук не используется
    (SchedulesPage делает inline-запросы напрямую к /shifts)
  — useOnCallNow: хук не используется (SchedulesPage использует useQueries inline)
  — useDeleteOverride: хук не используется (UI кнопки удаления замены нет)
  Удалить мёртвые хуки или добавить UI для их вызова (кнопка удаления в Gantt).

  ЗАМЕЧАНИЕ 4 (КОСМЕТИЧЕСКОЕ / расхождение с дизайном): date-fns-tz не используется
  ───────────────────────────────────────────────────────────────────────────────────
  Дизайн (раздел «Сложность Gantt-расписания») и задача 6.3 предписывают вычислять
  окна смен через `date-fns-tz` (конвертация UTC-границ смен в локальный часовой
  пояс). `date-fns-tz` указан в зависимостях, но НИГДЕ не импортируется (grep по src
  даёт 0 вхождений).

  Фактическая реализация в src/pages/SchedulesPage.tsx использует нативный
  `new Date(shift.start_at).getTime()` + `format()` из date-fns. Функционально это
  почти корректно: epoch-таймстампы абсолютны, а `format()` рендерит в локальном
  часовом поясе пользователя с учётом DST. Единственное расхождение — геометрия
  Gantt-полос: колонки дней имеют равную ширину `1/N`, тогда как месяц с переходом
  DST содержит день в 23ч/25ч, из-за чего полосы могут смещаться до ~1ч
  (~0.14% ширины) в месяцы перехода DST.

  Варианты:
  A. Провести конвертацию границ смен через `date-fns-tz` (`toZonedTime`) согласно
     дизайну; одновременно закрывает задачу 10.2 (юнит-тест DST окна смены).
  B. Зафиксировать в дизайне/задаче 6.3, что выбран нативный `Date`, и убрать
     `date-fns-tz` из зависимостей.
  Рекомендация: вариант A (соответствие дизайну + тестируемость DST).

  Задачи:
  ───────
  — [ ] F4.1 Исправить обработку 409 в CreateOverrideModal: использовать
        err.response?.data?.error вместо попытки форматировать несуществующие поля
  — [ ] F4.2 Исправить тип Override в src/api/types.ts: start_at/end_at вместо
        start/end, убрать username/display_name/created_by
  — [ ] F4.3 Исправить useScheduleWindow: изменить endpoint с /oncall на /shifts
        (или удалить хук, если SchedulesPage останется с inline-запросами)
  — [ ] F4.4 Добавить UI для удаления замены (кнопка на Gantt-баре для is_override)
        и подключить useDeleteOverride; или удалить хук если фича не нужна
  — [ ] F4.5 (Backend, backlog) Вернуть детали конфликта в теле 409:
        existing_start, existing_end, existing_user_id — для информативного
        сообщения пользователю
  — [ ] F4.6 Привести вычисление окна смены в соответствие с дизайном: провести
        конвертацию UTC-границ смен через `date-fns-tz` (`toZonedTime`) в
        src/pages/SchedulesPage.tsx (барам Gantt и подписям); либо зафиксировать
        в дизайне/задаче 6.3 выбор нативного `Date` и убрать `date-fns-tz` из
        зависимостей. Связано с задачей 10.2 (юнит-тест DST окна смены).
════════════════════════════════════════════════════════════════════════════ -->

## 7. UI политик эскалации

- [x] 7.1 Собрать `EscalationPoliciesPage` со списком политик: название, количество шагов, бейдж «По умолчанию» и кнопки действий (редактировать, удалить, сделать по умолчанию)
- [x] 7.2 Реализовать диалог подтверждения удаления; привязать к mutation `useDeletePolicy`
- [x] 7.3 Собрать `PolicyEditorPage` (создание/редактирование): вертикальный степпер, рендерящий каждый шаг политики как карточку
- [x] 7.4 Карточка шага: выпадающий список расписаний (из `useSchedules`), поле таймаута (целое число, мин. 1), кнопка удаления, стрелки вверх/вниз
- [x] 7.5 Добавить валидацию шагов: встроенная ошибка при не выбранном расписании; блокировка кнопки сохранения до заполнения всех шагов
- [x] 7.6 Привязать кнопку «Сохранить» к `useUpdatePolicy` или `useCreatePolicy` в зависимости от режима создания/редактирования; показывать toast об успехе
- [x] 7.7 Привязать действие «Сделать по умолчанию» к `useSetDefaultPolicy`; обновлять список для актуализации бейджа «По умолчанию»

<!-- ═══════════════════════════════════════════════════════════════════════════
  БУДУЩАЯ ФИЧА: Управление эскалацией инцидента
  Эндпоинты бекенда реализованы, фронтенд не реализован.
  Запланировать отдельным изменением (openspec new change incident-escalation-ui).

  Бекенд API (порт 8083):
    POST   /api/escalations/v1/{tenant}/incidents/{incidentId}/policy
           Тело: { policy_id: string, tenant_slug: string }
           Ответ: EscalationState — активирует политику для инцидента
    GET    /api/escalations/v1/{tenant}/incidents/{incidentId}/state
           Ответ: EscalationState { id, incident_id, tenant_id, policy_id,
                  current_tier, status, escalate_at, created_at, updated_at }
    POST   /api/escalations/v1/{tenant}/incidents/{incidentId}/escalate
           Тело: пустое — вручную продвигает на следующий уровень
           Ответ: обновлённый EscalationState
    GET    /api/escalations/v1/{tenant}/incidents/{incidentId}/history
           Ответ: EscalationHistory[] { id, incident_id, event_type,
                  tier, oncall_user_id, oncall_username, created_at }

  Что нужно сделать на фронте:
  — Хуки в src/api/escalations.ts:
      useEscalationState(tenant, incidentId)  → GET /state, поллинг 30s
      useAttachPolicy(tenant, incidentId)     → POST /policy
      useManualEscalate(tenant, incidentId)   → POST /escalate
      useEscalationHistory(tenant, incidentId)→ GET /history
  — Типы в src/api/types.ts:
      EscalationState, EscalationHistory (поля перечислены выше)
  — В IncidentDetailPanel (src/pages/IncidentDetailPanel.tsx):
      Новый раздел / вкладка «Эскалация»:
        · Виджет текущего состояния: текущий уровень (tier), статус,
          время следующей автоэскалации (escalate_at, обратный отсчёт)
        · Кнопка «Назначить политику» → модалка с select из useEscalationPolicies
          (POST /policy), показывать только если policy не назначена
        · Кнопка «Эскалировать сейчас» → POST /escalate с confirm-диалогом,
          активна только при status=active и current_tier < max_tier
        · Хронология: список событий из GET /history (event_type: triggered /
          tier_advanced / acknowledged / resolved / exhausted), аналог вкладки «История»
  — Статус escalation.status отображать в карточке инцидента в списке (опционально)
════════════════════════════════════════════════════════════════════════════ -->

## 8. UI настроек тенанта

- [x] 8.1 Собрать `TenantSettingsPage` с тремя секциями: Webhook-токены, Конфигурация уведомлений, Участники
- [x] 8.2 Секция Webhook-токенов: список из `useWebhookTokens`, кнопка «Отозвать» на каждой строке с диалогом подтверждения (привязать к `useRevokeToken`)
- [x] 8.3 Собрать `GenerateTokenModal`: поле ввода метки источника + отправка; при успехе показывать `OneTimeTokenRevealModal`
- [x] 8.4 Собрать `OneTimeTokenRevealModal`: копируемый `<pre>` с текстом токена, обязательный чекбокс «Я скопировал этот токен», кнопка «Закрыть» активна только при отмеченном чекбоксе; очищать токен из состояния при закрытии
- [x] 8.5 Секция конфигурации уведомлений: форма с `mattermost_webhook_url` (валидация URL), `mattermost_channel`, `smtp_from` (валидация email); сохранение через `useSaveNotificationConfig`
- [x] 8.6 Секция участников: список только для чтения из `useMembers`; статический информационный баннер об управлении составом команды через Keycloak

## 9. UI профиля пользователя

- [x] 9.1 Собрать `ProfilePage` по адресу `/profile` с формой контактов: поле имени пользователя в Mattermost, поле email (валидация формата)
- [x] 9.2 Привязать форму к `useUserContacts` (загрузка) и `useSaveUserContacts` (сохранение); показывать toast об успехе
- [x] 9.3 Добавить переключатели каналов уведомлений (email, mattermost), отражающие `enabled_channels`; переключение немедленно вызывает `useSaveUserContacts` с обновлённым массивом
- [x] 9.4 Реализовать гард: при включении переключателя Mattermost без указанного имени пользователя — показывать встроенное предупреждение и возвращать переключатель в исходное положение

<!-- ═══════════════════════════════════════════════════════════════════════════
  БУДУЩАЯ ФИЧА: Улучшение надёжности API-хуков и безопасность контактов

  КОНТЕКСТ
  ────────
  В ходе аудита API-слоя выявлены два замечания: одно по качеству обработки
  ошибок в хуках (заглушают любые ошибки, не только 404), второе — по
  серверной уязвимости, которую частично можно смягчить на стороне фронтенда.

  ЗАМЕЧАНИЕ 1: Хуки глотают все ошибки вместо только 404
  ────────────────────────────────────────────────────────
  Затронутые хуки:
    — `useUserContacts` в `src/api/profile.ts`
    — `useDefaultPolicy` в `src/api/escalations.ts`

  Текущее поведение: блок `catch {}` перехватывает любое исключение (500,
  сетевую ошибку, таймаут) и возвращает `null`, скрывая реальную проблему
  от React Query и пользователя.

  Нужное поведение:
    — Возвращать `null` только при HTTP 404
    — Пробрасывать остальные ошибки, чтобы React Query пометил запрос
      как `isError: true` и компонент мог показать сообщение об ошибке

  Реализация (оба хука):
    ```typescript
    import axios from 'axios'

    // в queryFn:
    try {
      const { data } = await apiClient.get(...)
      return data
    } catch (err) {
      if (axios.isAxiosError(err) && err.response?.status === 404) return null
      throw err
    }
    ```

  ЗАМЕЧАНИЕ 2: Бэкенд не проверяет userId == claims.Sub
  ───────────────────────────────────────────────────────
  `RequireTenantMember` проверяет только членство в тенанте, но не совпадение
  `{userId}` в URL с `sub` из JWT. Любой участник тенанта технически может
  читать и перезаписывать контакты другого участника.

  Это серверная дыра, полноценно устраняется только на бэкенде (добавлением
  проверки `userID == claims.Sub` в `PutContact`/`GetContact`). Со стороны
  фронтенда можно лишь убедиться, что мы никогда не конструируем URL с чужим
  userId. Текущая реализация ProfilePage уже делает это корректно — всегда
  передаёт `user.profile.sub`.

  Задачи:
  ───────
  — [ ] F2.1 Обновить `useUserContacts` и `useDefaultPolicy`: перехватывать
        только HTTP 404, остальные ошибки пробрасывать через `throw`
  — [ ] F2.2 Добавить обработку `isError` в `ContactsForm` и
        `EscalationPoliciesPage`: показывать inline-баннер ошибки при сбое
        загрузки (вместо молчаливого пустого состояния)
  — [ ] F2.3 (Backend) В `PutContact`/`GetContact` добавить проверку
        `userID == claims.Sub`, возвращать 403 при несовпадении
════════════════════════════════════════════════════════════════════════════ -->

<!-- ═══════════════════════════════════════════════════════════════════════════
  БУДУЩАЯ ФИЧА: Качество и сборка (бывшая секция 10) — сейчас НЕ делаем

  КОНТЕКСТ
  ────────
  Прикладной код секций 1–9 готов и собирается без ошибок (`tsc -b` проходит).
  Остаётся хардненинг: юнит-тесты, CI-гейт, инфраструктура раздачи. Перенесено
  в backlog по решению — сейчас не реализуем.

  СТАТУС НА МОМЕНТ ПЕРЕНОСА
  ────────────────────────
  — 10.5 и 10.6 ФАКТИЧЕСКИ УЖЕ СДЕЛАНЫ в коде:
      • frontend/nginx.conf — SPA fallback, MIME, cache-headers, gzip (10.5)
      • deploy/k8s/frontend/{deployment,service}.yaml + правило фронта в
        deploy/k8s/ingress.yaml (catch-all `/()(.*)` → frontend:80) (10.6)
    Требуют только проверки и формального закрытия, если решим вернуться.
  — 10.1–10.4 не сделаны: нет test-runner (vitest/jest отсутствует в
    package.json), нет тестовых файлов, нет CI-конфига (.github/.gitlab).

  Задачи:
  ───────
  — [ ] 10.1 Юнит-тесты парсинга claim `groups` в usePermissions() (несколько
        тенантов, отсутствующий groups, роль admin) — целевая логика parseGroups
        в src/auth/usePermissions.ts:10 уже выделена и тестопригодна
  — [ ] 10.2 Юнит-тесты вычисления окна смены в Gantt (включая граничный случай
        DST) — целевая функция barPosition в src/pages/SchedulesPage.tsx:46;
        связано с F4.6 (date-fns-tz)
  — [ ] 10.3 Юнит-тесты триггера аудиосигнала нового инцидента (обнаружение новых
        ID со статусом `open`) — логика в src/pages/IncidentListPage.tsx:83
  — [ ] 10.4 Добавить `vite build` в CI pipeline; падать при ошибках компиляции
        TypeScript (CI-конфига сейчас нет; сама сборка проходит чисто)
  — [ ] 10.5 (уже реализовано — проверить и закрыть) nginx.conf: SPA fallback,
        MIME-типы, cache-headers для статики — frontend/nginx.conf
  — [ ] 10.6 (уже реализовано — проверить и закрыть) Deployment + Service +
        Ingress фронтенда — deploy/k8s/frontend/* и deploy/k8s/ingress.yaml
════════════════════════════════════════════════════════════════════════════ -->
